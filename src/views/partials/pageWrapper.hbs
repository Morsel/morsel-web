<div id="page-wrapper" class="page-wrapper" ng-class="{'menu-open':menuOpen}">
  {{> sidenav}}
  <div id="site" class="site">
    <div ui-view="main" class="main-view" ng-click="closeMenu()">
      {{#staticPartial.about}}
      {{> static/about}}
      {{/staticPartial.about}}
      {{#staticPartial.apps}}
      {{> static/apps}}
      {{/staticPartial.apps}}
      {{#staticPartial.press}}
      {{> static/press}}
      {{/staticPartial.press}}
      {{#staticPartial.privacy}}
      {{> static/privacy}}
      {{/staticPartial.privacy}}
      {{#staticPartial.terms}}
      {{> static/terms}}
      {{/staticPartial.terms}}
    </div>
  </div>
</div>