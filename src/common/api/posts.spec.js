describe( 'API Posts section', function() {
  beforeEach( module( 'Morsel.apiPosts' ) );

  it( 'method getposts should return a list of posts', inject( function() {
    expect( true ).toBeTruthy();
  }));
});

